// AuthHandler is no longer needed as user data is handled in AuthContext
// This file can be removed or kept empty for backwards compatibility
const AuthHandler = () => {
  return null;
};

export default AuthHandler;
